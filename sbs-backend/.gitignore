# Dependencies
node_modules/
/dist

# Environment variables
.env
*.env
.env.*
!.env.example

# Documents (ignored globally, as per your preference)
.pdf
.docx
.zip

# OS files
.DS_Store
Thumbs.db

# -- NEW SAFE ADDITIONS BELOW --

# Instead of ignoring public/ entirely, ignore only PDFs inside it:
public/**/*.pdf
public/**/*.docx
public/**/*.zip

# Dev caches & logs (essential to add)
*.log
*.tsbuildinfo
.eslintcache
.prettiercache
coverage/